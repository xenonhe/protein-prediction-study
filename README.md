# Protein Prediction Study

Personal study repository — working through deep learning models for protein prediction
based on the review paper: *[Add paper title & citation here]*.

## Structure
- `models/` — One folder per model, each with a tutorial notebook
- `embeddings/` — Reusable embedding utilities (ESM-2, one-hot, etc.)
- `data/` — Raw and processed datasets (not tracked by git)
- `utils/` — Shared helpers for metrics and visualization
- `notes/` — Reading notes from the paper

## Progress
| Model | Status | Dataset Used | Embedding |
|-------|--------|-------------|-----------|
| _template | 🔲 | - | - |

## Setup
```bash
conda env create -f environment.yml
conda activate protein-study
```
